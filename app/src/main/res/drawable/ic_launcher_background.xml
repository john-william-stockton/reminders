<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Matches the M3 baseline surface tone for the app's purple color scheme (see the
         commented-out `background` override in Theme.kt) so the icon reads as part of the
         same palette as the app itself, rather than a generic white or the old template green. -->
    <path
        android:fillColor="#FFFBFE"
        android:pathData="M0,0h108v108h-108z" />
</vector>
