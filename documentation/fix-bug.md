# Fix Bug

## Steps
1. Grab first `Open` bug from `./BUGS.md`, create an organized bug-fix plan for the prompter to review. 
    If no more bugs, analyze the code for evident and potential bugs, add them to the list (upon approval), and repeat this step.
2. Only implement once explicitly granted permission.
3. Prompt the user to manually verify the fix, and then ensure `./BUGS.md` has been updated appropriately and commit and push changes only once explicitly granted permission.